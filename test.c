#include <stdio.h>
#include <string.h>
#include <time.h>


int main(int argc, char *argv[])
{
	size_t spn = 0;
	size_t date;

		date = (size_t)time(0);
		printf ("time : %lu\n", date);
	
	return (0);
}
