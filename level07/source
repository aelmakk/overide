#include <stdio.h>
#include <string.h>

read_number()
{

}

store_number()
{

}

main(int argc, char *argv[])
{
    char              buff[400]; //confirmed that buff size is 400
    4byts_size        var1;
    4byts_size        var2;
    4byts_size        var3;
    4byts_size        var4;
    4byts_size        var5;
    4byts_size        var6;
    char       *tmp;
    
    // tmp = av[0];
    bzero(buff, sizeof(int) * 0x64);
    strlen(argv[0]); //    "/home/users/level07/level07"  ==> 27
    memset(argv[0], '\0', 27);
    // weird shit 

    puts("----------------------------------------------------
            \n  Welcome to wil\'s crappy number storage service!   \n
            ----------------------------------------------------
            \n Commands:
            \n      store - store a number into the data storage
            \n      read  - read a number from the data storage
            \n      quit  - exit the program
            ----------------------------------------------------
                  wil has reserved some storage :>
            ---------------------------------------------------");
    printf("Input command: ");
    return (0);
}